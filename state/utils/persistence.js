/* import { AsyncStorage } from 'react-native';
import { APP_STORAGE_KEY } from '../../constants'; */

/**
 * Function to load the app state from AsyncStorage.
 * @returns {Promise} Promise object that represents the state persisted
 * in the device.
 */
/* const loadState = () => AsyncStorage.getItem(APP_STORAGE_KEY); */

/**
 * Function to persist the app state into AsyncStorage.
 * @returns {Promise}
 */
/* const persistState = (state) => {
  const serializedState = JSON.stringify(state);
  return AsyncStorage.mergeItem(APP_STORAGE_KEY, serializedState);
}

export {
  loadState,
  persistState,
}; */
